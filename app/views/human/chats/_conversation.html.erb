<div class="card h-100 shadow-sm rounded-4 human-chat-conversation">
  <div class="card-header bg-white border-0 d-flex align-items-center">
    <% counterpart = chat.client == current_user ? chat.supplier : chat.client %>
    <div class="me-3">
      <% if counterpart&.user_photo&.attached? %>
        <%= cl_image_tag counterpart.user_photo.key,
                         width: 48, height: 48,
                         crop: "fill", gravity: "auto",
                         class: "rounded-circle object-fit-cover" %>
      <% else %>
        <div class="avatar-placeholder rounded-circle text-center fw-bold p-1"
             style="background-color: <%= avatar_bg_color_for(counterpart) %>; color: #0f172a;">
          <%= counterpart&.first_name&.first || "?" %>
        </div>
      <% end %>
    </div>
    <div>
      <div class="fw-bold fs-5 text-primary"><%= counterpart&.first_name || "Usuario" %></div>
      <div class="text-muted"><%= chat.service.sub_category %></div>
    </div>
  </div>

  <div class="card-body d-flex flex-column gap-3 overflow-auto flex-grow-1">
    <% if messages.any? %>
      <% messages.each do |message| %>
        <div class="chat-bubble <%= 'from-me ms-auto text-white bg-primary' if message.user == current_user %>">
          <%= simple_format(message.content, {}, wrapper_tag: "span") %>
          <div class="text-end text-muted small mt-1">
            <%= l message.created_at, format: :short %>
          </div>
        </div>
      <% end %>
    <% else %>
      <div class="text-center text-muted my-5">
        Todavía no hay mensajes en este chat.
      </div>
    <% end %>
  </div>

  <div class="card-footer bg-white border-0">
    <%= form_with model: [chat, Human::Message.new],
                  url: human_chat_messages_path(chat),
                  data: { turbo_frame: "human_chats_conversation" },
                  class: "chat-composer" do |f| %>
      <div class="chat-input-wrapper">
        <%= f.text_area :content,
                        class: "form-control chat-input",
                        rows: 2,
                        placeholder: "Escribí tu mensaje..." %>
        <button type="submit" class="chat-send-btn" aria-label="Enviar">
          <i class="bi bi-arrow-up-short"></i>
        </button>
      </div>
    <% end %>
  </div>
</div>
