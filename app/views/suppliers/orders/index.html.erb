<div class="container-fluid py-4 supplier-dashboard">
  <div class="row g-3">
    <!-- Sidebar -->
    <aside class="col-12 col-lg-3">
      <div class="card shadow-sm sidebar-card sticky-lg-top">
        <div class="card-body">
          <div class="d-flex align-items-center mb-3">
            <% if current_user&.user_photo&.attached? %>
              <%= image_tag current_user.user_photo, class: "rounded-circle avatar-sm flex-shrink-0", alt: "#{current_user.first_name}" %>
            <% else %>
              <div class="rounded-circle bg-secondary-subtle text-secondary-emphasis d-inline-flex align-items-center justify-content-center avatar-sm flex-shrink-0">
                <span class="fw-semibold"><%= current_user.first_name %></span>
              </div>
            <% end %>
            <div class="ms-2">
              <h6 class="mb-0"><%= current_user.first_name %></h6>
              <small class="text-muted">Proveedor</small>
            </div>
          </div>

          <nav class="nav flex-column nav-pills gap-1">
            <%= link_to "Mis Servicios", suppliers_dashboard_index_path, class: "nav-link d-flex align-items-center hover-dashboard" %>
            <%= link_to "Ordenes Pendientes", suppliers_orders_path(pending: true), class: "nav-link d-flex align-items-center hover-dashboard" %>
            <%= link_to "Historial de Órdenes", suppliers_orders_path(history: true), class: "nav-link d-flex align-items-center hover-dashboard" %>
            <%= link_to "Mis Calificaciones", "", class: "nav-link d-flex align-items-center #{'active' if controller_name == 'reviews'} hover-dashboard" %>

          </nav>
        </div>

        <div class="card-footer bg-white">
          <ul class="list-unstyled small mb-0">
            <li class="mb-2">
              <%= link_to "Ayuda", "", class: "text-decoration-none d-inline-flex align-items-center gap-2" %>
            </li>
            <li>
              <%= link_to "Cuenta", edit_user_registration_path, class: "text-decoration-none d-inline-flex align-items-center gap-2" %>
            </li>
          </ul>
        </div>
      </div>
    </aside>

    <!-- Main -->
    <main class="col-12 col-lg-9">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h2 text-primary mb-0">Ordenes</h1>
      </div>

      <div class="card shadow-sm p-2">
        <div class="table-responsive">
          <table class="table align-middle mb-0">
            <thead class="table-light">
              <tr>
                <th class="text-center bg-primary">Fecha</th>
                <th class="text-center bg-primary">Servicio</th>
                <th class="text-center bg-primary">Cliente</th>
                <th class="text-center bg-primary">Precio Total</th>
                <th class="text-center bg-primary">Estado</th>
                <th class="text-center bg-primary">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% current_user.services.each do |service| %>
                <% selected_orders = service.orders.select { |order| order.status == params[:status] } %>
                <% selected_orders.each do |order| %>
                  <tr class="service-row">
                    <td class="text-muted text-center"><%= order.start_time %></td>
                    <td class="fw-semibold text-center">
                      <%= order.service.sub_category %>
                    </td>
                    <td class="text-muted text-center"><%= order.user.first_name %></td>
                    <td class="text-center"><%= number_to_currency(order.total_price, unit: "$", precision: 0) %></td>
                    <td class="text-center">
                      <%= order.status %>
                    </td>
                    <td class="text-end">
                      <%= link_to "Ver", order_path(order), class: "link-danger fw-semibold" %>
                      <%= link_to "Editar", edit_order_path(order), class: "link-primary fw-semibold" %>
                      <%= link_to "Reseña", new_order_review_path(order), class: "link-success fw-semibold mx-2" %>
                    </td>
                  </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </main>
  </div>
</div>
