<div class="container d-flex align-items-center justify-content-center h-100">
  <div class="col-md-8 my-4">
    <div class="d-flex justify-content-between bg-white border rounded-4 p-3 my-3">
      <h2 class="text-center mb-3">Historial</h2>
      <div class="mb-4">
        <%= simple_form_for @chat do |f| %>
          <%= f.submit "Nuevo chat con tu Asitente IA", class: "btn btn-primary" %>
        <% end %>
      </div>
    </div>

    <% if @chats.empty? %>
      <div class="text-center text-muted mt-5">
        <p>No tienes chats con tu Asistente de IA. ¡Empieza uno ahora!</p>
      </div>
    <% end %>
        <% @chats.each do |chat| %>
          <!-- Para que el preview funcione, he dejado una card de ejemplo. En tu app, esta card estaría dentro del bucle. -->
          <div class="card bg-white mb-3 rounded-4 shadow-sm">
                  <!-- Se agregaron clases de flexbox para alinear el título y la fecha, y se redujo el padding (py-2) -->
                <%= link_to chat_path(chat), class: "card-link-wrapper fs-7 text-decoration-none" do %>
                  <div class="card-body d-flex justify-content-between align-items-center py-2 px-3">
                      <!-- Reemplaza 'chat.title' con el atributo correcto de tu modelo -->
                      <h5 class="card-title mb-0"><%= chat.title %></h5>

                      <!-- 'time_ago_in_words' es un helper de Rails muy útil para esto -->
                      <p class="card-text text-muted small mb-0">
                        Started <%= time_ago_in_words(chat.created_at) %> ago
                        <%= link_to "Eliminar",
                        chat_path(chat),
                        class: "text-decoration-none text-warning mx-2 fw-semibold",
                        data: { turbo_method: :delete, turbo_confirm: "¿Estás seguro de eliminar este chat?" } %>
                      </p>
                  </div>
                <% end %>
              </div>
        <% end %>

  </div>

</div>            <%# Si no hay chats, puedes mostrar un mensaje alternativo %>


<%#= console %>
